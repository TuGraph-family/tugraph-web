import http from '../../core/http'

interface options {
    [propName: string]: any
}

export function getUserInfo(param: string) {
    // let api_url = '/acl'
    // let options: options = {
    //     headers: {
    //         Authorization: localStorage.__FMA_TOKEN__
    //     }
    // }
    // return http.get(api_url, { user: param }, options)
}
